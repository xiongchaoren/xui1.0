<!--templates/test/test.wxml-->
<template name="test">
    <view class='pull'>
        <view>样式二:</view>
        <view class='pull-item' wx:for="{{_content}}">{{item.name}}</view>
    </view>
</template>
