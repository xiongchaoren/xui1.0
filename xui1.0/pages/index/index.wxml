<!--index.wxml-->
<view class='page'>

    <view class='page__hd'>XUI---小程序UI框架</view>

    <view class='page__bd'>
        <view class='page__bd-title'>基础</view>
        <block wx:for="{{menu}}" wx:key="*this">
            <navigator url="{{item.path}}" class='menu-item'>{{item.type}} {{item.name}}</navigator>
        </block>
    </view>
    <view class='page__bd'>
        <view class='page__bd-title'>操作反馈</view>
        <block wx:for="{{menu2}}" wx:key="*this">
            <navigator url="{{item.path}}" class='menu-item'>{{item.type}} {{item.name}}</navigator>
        </block>
    </view>
    
</view>